import React from 'react';
import { Container } from 'react-bootstrap';
import PageHeader from '../PageHeader';

const AboutUS = () => {
    return (
        <Container>
            <PageHeader bigTitle="Sobre" smallTitle="nós" />
        </Container>
    );
};

export default AboutUS;